#include<stdio.h>
int main(){
    int n;
    float f;
    double d;
    char c;
    printf("size of int %ld, float %ld, double %ld, char %ld\n", sizeof(n), sizeof(f) , sizeof(d), sizeof(c));
    return 0;
}